id: arbiter
name: Arbiter
role: tool
version: v0.1.0
description: Centralized architectural enforcement engine — evaluates ADR-047 invariants before packaging and execution
runtime:
  binary: arbiter
  port: 9094
  env:
    ARBITER_HTTP_ADDR: "127.0.0.1:9094"
    ARBITER_SERVICE_TOKEN: ""  # set to platform service token in production
